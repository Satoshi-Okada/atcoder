#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <cmath>
#include <math.h>
using namespace std;

struct set{
    int a;
    int b;
};

int main()
{
    int n,m;
    cin >> n >> m;
    vector<set> vc;
    for (int i = 0; i < n; i++)
    {
        cin >> vc[i].a >> vc[i].b;
    }

    sort()
    long ans;
    
    
    

    
}